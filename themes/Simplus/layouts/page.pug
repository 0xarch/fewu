include /mixins/script.pug
include /mixins/pages.pug
doctype html
html 
    head 
        +meta(user.note,i18n('Blogs'))
        +base()
        title= __title__(i18n('Blogs'),user.name)
    body(OCP='OVERRIDE') 
        +header()
        #main
            .content
                each bid in cycling.value
                    +post(ID[bid])
                +foot_navigator(cycling.total,cycling.now,'page')
            +sidebar()
        include /includes/footer.pug
        include /components/searchPanel.pug