.card.about.column
    .flex.column.info-1
        img(src=user.avatar,alt=i18n('User Avatar'))
        .name=user.name
        .describe=user.note
        .location=user.location
    .flex.p8.jcc
        a.followme(href=user.url)= i18n('Follow')
    .flex.info-2
        .posts
            span= i18n('Articles')
            p= site.posts.length
        .categories
            span= i18n('Categories')
            p= site.category_count
        .tags
            span= i18n('Tags')
            p= site.tag_count