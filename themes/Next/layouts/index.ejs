<!DOCTYPE html>
<html>
<%- include("includes/head.ejs",{title:{title:"首页",titleType:"index"}}) %>
<body>
    <%-include("includes/drawer.ejs")-%>
    <Toolbar>
        <button id="binding-d1" class="icon-button hide-on-wide">
            <icon>menu</icon>
        </button>
    </Toolbar>
    <Paper>
        <div class="d-post-container">
            <% Sorts.RecentPosts.forEach((item)=>{ -%>
                <%- include("includes/components/postcard.ejs",{item:BID[item]}) %>
            <%})-%>
        </div>
    </Paper>
</body>
    <script type="module" src="<%=ROOT-%>/files/meui.js"></script>
    <script>
        document.querySelectorAll(".d-post-date").forEach((i)=>{
            let date = new Date(i.innerHTML);
            i.innerHTML = date.toLocaleDateString();
        });
    </script>
</html>