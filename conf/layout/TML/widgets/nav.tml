<nav'[fI flAC]>
    !span'[blogName].in( &builtin::siteTitle );
    <span.navItem>
        !div'[menuItems mr].in(!a.href(/).in(!i'[fa-fw fas fa-home].in();首页););
        !div'[menuItems mr].in(!a.href(/archive/).in(!i'[fa-fw fas fa-archive].in();归档););
        !div'[menuItems mr].in(!a.href(/category/).in(!i'[fa-fw fas fa-folder-open].in();分类););
        !div'[menuItems mr].in(!a.href(/about/).in(!i'[fa-fw fas fa-heart].in();关于););
    </span>
    !span'[navButton mh mw].in(!i'[fas fa-fw fa-bars].in(););
</nav>