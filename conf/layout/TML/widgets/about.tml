!div'[widget about]{
    !div'[centered]{
        !img'[avatar].src(@info.avatar@);
        !h3{ !a.href(@info.url@){@info.name@} }
        !h5{@info.note@}
    }
    !div'[centered info-data]{
        !div'[counting-post fR flJC]{
            !div'[postCount mr]{
                !span{文章}
                !span'[count]{@ginfo.postCount@}
            }
            !div'[cateCount ml]{
                !span{分类}
                !span'[count]{@ginfo.cateCount@}
            }
        }
    }
}