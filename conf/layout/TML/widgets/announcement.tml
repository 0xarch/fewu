<if:info.features.hasAnnouncement>
    <div'[widget announcement]>
        !div'[header].in( !i'[fas fa-bullhorn].in(); !span.in(公告); );
        !div'[content].in( !span.in(@info.features.announcementText@); );
    </div>
</if>